import React from 'react';
import Page from 'components/Page/Page';

const NotFoundPage = () => {
    return (
        <Page.Error>
            404
        </Page.Error>
    )
};

export default NotFoundPage;
